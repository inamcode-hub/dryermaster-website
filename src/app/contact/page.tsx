import React from 'react';
import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Dryer Master | Contact Us',
  description: '',
};
const Contact = () => {
  return <section>Contact</section>;
};

export default Contact;
